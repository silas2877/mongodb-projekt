apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb-project.fullname" . }}
  labels:
    {{- include "mongodb-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodb-project.selectorLabels" . | nindent 4 }}
